# PlugIns

The cores and system plugins the app loads. `Scripts/cassowary/build-cassowary.sh` fills
this directory before generating the Xcode project; the contents are build
products and are not checked in.
