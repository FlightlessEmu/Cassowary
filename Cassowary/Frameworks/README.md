# Frameworks

The Cassowary (iOS) builds of `OpenEmuBase`, `OpenEmuSystem`, `OpenEmuShaders` and
`OpenEmuKit` are copied here by `Scripts/cassowary/build-cassowary.sh` before the Xcode
project is generated. They are build products and are not checked in.
