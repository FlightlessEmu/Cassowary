# Xcode
.DS_Store
OEBuildVersion.h
build/
*.pbxuser
!default.pbxuser
*.mode1v3
!default.mode1v3
*.mode2v3
!default.mode2v3
*.perspectivev3
!default.perspectivev3
*.xcworkspace
!OpenEmu-metal.xcworkspace
!default.xcworkspace
xcuserdata/
*.xccheckout
*.xcuserstate
*.xcscmblueprint
profile
*.moved-aside
DerivedData/
CodeSign.xcconfig
Build/
DerivedData/
build_*.log
build_*.txt
build_log*.txt
*.tar.gz
Releases/
!Releases/*.md


# Misc build artifacts committed to root
hang_sample.txt
dlopen_result.txt
link_error.log
broker_build.log
kit_build.log
retail_build.log
vice_build.log
*.zip

# Compiled test binaries
test_dlopen

# Claude Code local config
.claude/settings.json

# Cursor IDE config — local user settings stay out of git, but project hooks
# are checked in so every contributor (and AI session) gets the same automated
# guardrails.
.cursor/*
!.cursor/hooks.json
!.cursor/hooks/

# Agent artifacts — plans, scratchpads, notes (kept out of PR diffs by convention)
tmp/agent/

build.log

# Xcode project file backups — generated by Xcode during manual edits
*.pbxproj.bak

cores/Flycast/flycast/core/deps/rcheevos/.build/

# iOS test output
build/ios-test-shots/
__pycache__/

# ScreenScraper app key — never commit credentials
Cassowary/Resources/ScreenScraperDevCredentials.plist
